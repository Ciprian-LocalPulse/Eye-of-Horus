# Compiler

Status: planned.

Eye of Horus does not yet have a compiler. The likely first implementation is an interpreter with a parser, AST, spatial matrix, and pulse runtime.

A compiler or bytecode VM may be introduced only after the language core is stable enough to justify additional layers.
