# Virtual Machine

Status: research idea.

A VM is not implemented. It may become useful if the interpreter needs an intermediate representation for portability, visualization, testing, or optimization.

Future RFCs should justify whether a VM exists and what semantics it preserves.
